# for문과 range 함수
# 1 부터 10까지 더하는 것

add = 0

for i in range(1,11):
    add = add + i

print(add)

# range - 숫자 리스트를 자동으로 만들어 주는 함수
# range(a) : 0부터 a 미만의 숫자를 포함하는 range 객체 만들어줌
# range(a, b) : a 부터 b 미만의 숫자 